ota:
  - platform: esphome
    id: ota_esphome
    password: !secret password
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: ${name}
    password: "yourstrongpasword"
captive_portal:
web_server:
  port: 80
logger:
  level: DEBUG
  logs:
    scheduler: DEBUG
time:
  - platform: homeassistant
    id: homeassistant_time
